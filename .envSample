# Gemini API Key
GOOGLE_GENERATIVE_AI_API_KEY=KEY_HERE
GEMINI_API_KEY=KEY_HERE
UPLOADTHING_TOKEN=KEY_HERE
export REPLICATE_API_TOKEN=KEY_HERE
UPLOADTHING_APP_ID=KEY_HERE

# Note: Replace 'your_gemini_api_key_here' with your actual Gemini API key
# The NEXT_PUBLIC_ prefix allows this variable to be accessible in browser code
# If you only need the API key on the server side, remove the NEXT_PUBLIC_ prefix 